#include<bits/stdc++.h>
using namespace std;
int sortArr(int arr[],int size){
    sort(arr,arr+size);
    return arr[size-1];
}
int main(){
    int arr[] = {2,5,1,3,0};
    int size = sizeof(arr)/sizeof(arr[0]);
    cout<<sortArr(arr,size)<<endl;
    return 0;
}
